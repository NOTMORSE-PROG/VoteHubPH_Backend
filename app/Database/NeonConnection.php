<?php

namespace App\Database;

use Illuminate\Database\PostgresConnection;

class NeonConnection extends PostgresConnection
{
    // Extends PostgresConnection with any Neon-specific functionality
    // Currently, the connector handles the connection, so this is just a placeholder
}
